{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Timesheet creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('karis_timesheet_timesheet') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
